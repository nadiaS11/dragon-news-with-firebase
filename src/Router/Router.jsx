import { createBrowserRouter } from "react-router-dom";

const routes = createBrowserRouter([
  {
    path: "/",
    element: <div className="font-poppins text-4xl">Hello world!</div>,
  },
]);

export default routes;
